node_modules/
dist/
.vite/
npm-debug.log*
vite-dev.*.log
wrangler-dev.*.log
# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example




# Local DB dumps / backups (may contain user PII) — never commit
backups/

# Test coverage output
coverage/

# Playwright generated artifacts + stored auth state (session cookies — never commit)
tests/e2e/.auth/
tests/e2e/.output/
tests/e2e/.report/
test-results/
playwright-report/
